# Private SSH key — NEVER commit this
n8n-key.pem

# Auto-generated Lambda zip — rebuilt by Terraform at plan time
lambda.zip

# Terraform working directory — downloaded providers and modules
.terraform/
.terraform.lock.hcl

# Your real variable values — contains your IP address
# The .example file IS committed; this file is NOT
terraform.tfvars
.env

# direnv — contains AWS credentials, never commit
.envrc

# Crash log
crash.log
